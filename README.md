# secondcolab
1. **Структура данных** - способ организации и хранения данных в памяти. Нужны чтобы эффективно хранить, искать и обрабатывать данные.
2. **Множества поддерживают** add, remove, объединение (union), пересечение (intersection), разность (difference). Используют для удаления дубликатов и проверки принадлежности элемента.
3. **Пустой словарь**: d = {} или d = dict(). Пустое множество: s = set() - нельзя писать s = {}, это создаст словарь.
4. **Пустой словарь**: d = {} или d = dict(). Пустое множество: s = set() - нельзя писать s = {}, это создаст словарь.
5. **append() добавляет один элемент в конец, extend() добавляет все элементы из другого списка:**

pythona = [1, 2]
a.append([3, 4])   # [1, 2, [3, 4]]
a.extend([3, 4])   # [1, 2, 3, 4]

**6. Использовать метод get():**

pythond = {"имя": "Александр"}
d.get("возраст", "не указан")  # вернёт "не указан" вместо ошибки

**7. Списковые включения - компактный способ создать список в одну строку:**

pythonsquares = [x**2 for x in range(10)]
Преимущества: короче, читабельнее, быстрее обычного цикла.

**8. Кортежи вместо списков используют когда данные не должны меняться:**


координаты точки (55.7, 37.6)
RGB цвет (255, 0, 0)
возврат нескольких значений из функции return name, age
